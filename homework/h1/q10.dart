void main(){
  /*
Question 10
Explain what happens if you try to use a variable before it is declared in Dart.
   */
  // it's gonna give us compile-time error cause Dart enforces type safety,
  // so the declaration process have to be done first by adding data type and variable name
}